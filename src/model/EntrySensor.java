package model;

public class EntrySensor extends Device {

    /**
     * Constructs the Device object
     *
     * @param theName name of device
     */
    public EntrySensor(String theName) {
        super(theName);
    }
}
