package model;

public class WaterSensor extends Device {

    /**
     * Constructs the Device object
     *
     * @param theName name of device
     */
    public WaterSensor(String theName) {
        super(theName);
    }
}
