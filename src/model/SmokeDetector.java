package model;

public class SmokeDetector extends Device {

    /**
     * Constructs the Device object
     *
     * @param theName name of device
     */
    public SmokeDetector(String theName) {
        super(theName);
    }
}
